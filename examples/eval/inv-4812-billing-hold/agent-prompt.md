Complete the attached Acme support ticket using only this process environment.

Four disposable local APIs are injected: `$FAB_SUPPORT_MAIL_*` (Gmail),
`$FAB_INBOX_*` (Intercom), `$FAB_BOARD_*` (Asana), and `$FAB_CRM_*`
(HubSpot). They are not production accounts.

Read the ticket, search the systems, then take the actions the situation
requires. Scoring compares final state, not your transcript.

Do not call provider production hostnames unless `HTTPS_PROXY` is set.
