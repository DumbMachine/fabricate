---
title: Pipedrive
description: Persons, organizations, and deals.
sidebar:
  icon: https://logos.composio.dev/api/pipedrive
---

Persons, organizations, and deals. Local Pipedrive v2 that keeps CRM
state across requests.

## Integration

| | Details |
| --- | --- |
| API | Pipedrive REST v2 |
| Auth | Synthetic bearer token |
| Hosts | `api.pipedrive.com` |
| Direct URL | Yes |
| Transparent proxy | Yes |

### Start it with a direct URL

Use `fab run` without `--proxy`. The command below downloads Fabricate's
example manifest directly, so you do not need to clone this repository.

```bash
curl -fsSL https://raw.githubusercontent.com/DumbMachine/fabricate/main/environments/acme-pipedrive.yaml | \
  fab run /dev/stdin -- \
    sh -c 'curl -sS -H "Authorization: Bearer $FAB_PIPEDRIVE_TOKEN" "$FAB_PIPEDRIVE_URL/api/v2/persons"'
```

The service is named `pipedrive` in the example manifest, so the variables are
`FAB_PIPEDRIVE_URL` and `FAB_PIPEDRIVE_TOKEN`.

### Start it through the proxy

Use this when your app uses the normal Pipedrive URLs. This one-paste check
calls Pipedrive's production hostname. Omit `$FAB_PIPEDRIVE_TOKEN`; the proxy replaces `Authorization` on that host with the environment's ephemeral token.

```bash
curl -fsSL https://raw.githubusercontent.com/DumbMachine/fabricate/main/environments/acme-pipedrive.yaml | \
  fab run /dev/stdin --proxy -- \
    curl -sS https://api.pipedrive.com/api/v2/persons
```

Fabricate handles `api.pipedrive.com` locally. Other hosts are forwarded unchanged.

## Scenarios

<ResourceScenarios
  resource="pipedrive"
  descriptions={{
    "pipedrive.minimal.v1": "Empty Pipedrive state for a clean test.",
    "pipedrive.acme-pipeline.v1": "Dana at Northwind, INV-4812, Contoso SSO, and Fernworks.",
  }}
/>

## Compatibility verification

Fabricate works through any client that can reach an API: `curl`, application
code, or an official SDK. We continuously test integrations to ensure they
behave as expected. The latest Pipedrive check ran against the Acme Pipedrive
environment with curl:

<IntegrationCompatibility resource="pipedrive" />

## Supported operations

<SupportedOperations resource="pipedrive" />
