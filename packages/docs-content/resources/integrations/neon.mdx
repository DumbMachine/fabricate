---
title: Neon
description: A reproducible Neon project API for local application testing.
sidebar:
  icon: https://logos.composio.dev/api/neon
---

import compatibility from "../_generated/neon.compatibility.json";
import example from "../_generated/neon-list-projects.json";

Fabricate ships a Neon v1 resource with a local API driven by the official Neon OpenAPI document and a synthetic bearer token. Every operation in that document is implemented. The included Acme Neon environment seeds a SaaS-sized Postgres estate (projects, branches, endpoints) plus the shared Acme investigation needles. Gmail, Asana, and HubSpot run in the same environment so agents can join those facts across products.

## Scenarios

| Scenario | What it gives you |
| --- | --- |
| `neon.minimal.v1` | Empty Neon state for a clean test. |
| `neon.acme-projects.v1` | Acme Postgres estate: projects, branches, and endpoints. Needles: `acme-prod` checkout/billing, INV-4812 ledger, Contoso `acme-sso`, fernworks analytics, Adatum DNS, LakeTech webhooks, Fabrikam SCIM. |

## Integration options

| Option | Supported | Use it when |
| --- | --- | --- |
| Direct base URL | Yes | Your client can use a local URL or root URL. |
| Transparent proxy | Yes | Your client must keep Neon’s normal hostnames. |

### Start it with a direct URL

Use `fab run` without `--proxy`. The command below downloads Fabricate's
example manifest directly, so you do not need to clone this repository.

```bash
curl -fsSL https://raw.githubusercontent.com/DumbMachine/fabricate/main/environments/acme-neon.yaml | \
  fab run --environment /dev/stdin -- \
    sh -c 'curl -sS -H "Authorization: Bearer $FAB_NEON_TOKEN" "$FAB_NEON_URL/api/v2/projects"'
```

The service is named `neon` in the example manifest, so the variables are
`FAB_NEON_URL` and `FAB_NEON_TOKEN`.

The response is the Acme project list:

<CommandOutput example={example} showCommand={false} />

### Start it through the proxy

Use this when your app uses the normal Neon URLs. This one-paste check
calls Neon's production hostname; the proxy supplies the local token.

```bash
curl -fsSL https://raw.githubusercontent.com/DumbMachine/fabricate/main/environments/acme-neon.yaml | \
  fab run --environment /dev/stdin --proxy -- \
    curl -sS https://console.neon.tech/api/v2/projects
```

Fabricate handles `console.neon.tech` locally. Other hosts are forwarded unchanged.

## Compatibility verification

Fabricate works through any client that can reach an API: `curl`, application
code, or an official SDK. We continuously test integrations to ensure they
behave as expected. The latest Neon check ran against the Acme Neon
environment with curl:

<IntegrationCompatibility compatibility={compatibility} />
