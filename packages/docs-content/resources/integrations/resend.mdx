---
title: Resend
description: A reproducible Resend email API for local application testing.
sidebar:
  icon: https://logos.composio.dev/api/resend
---

import compatibility from "../_generated/resend.compatibility.json";

Fabricate ships a Resend v1 resource with a strict local API and a
synthetic bearer token. It covers emails and domains in the included Acme Resend
environment.

## Scenarios

| Scenario | What it gives you |
| --- | --- |
| `resend.minimal.v1` | Empty Resend state for a clean test. |
| `resend.acme-outbox.v1` | Dana at Northwind, INV-4812, Contoso SSO, and Fernworks. |

## Integration options

| Option | Supported | Use it when |
| --- | --- | --- |
| Direct base URL | Yes | Your client can use a local URL or root URL. |
| Transparent proxy | Yes | Your client must keep Resend’s normal hostnames. |

### Start it with a direct URL

Use `fab run` without `--proxy`. The command below downloads Fabricate's
example manifest directly, so you do not need to clone this repository.

```bash
curl -fsSL https://raw.githubusercontent.com/DumbMachine/fabricate/main/environments/acme-resend.yaml | \
  fab run --environment /dev/stdin -- \
    sh -c 'curl -sS -H "Authorization: Bearer $FAB_RESEND_TOKEN" "$FAB_RESEND_URL/emails"'
```

The service is named `resend` in the example manifest, so the variables are
`FAB_RESEND_URL` and `FAB_RESEND_TOKEN`.

### Start it through the proxy

Use this when your app uses the normal Resend URLs. This one-paste check
calls Resend's production hostname; the proxy supplies the local token.

```bash
curl -fsSL https://raw.githubusercontent.com/DumbMachine/fabricate/main/environments/acme-resend.yaml | \
  fab run --environment /dev/stdin --proxy -- \
    curl -sS https://api.resend.com/emails
```

Fabricate handles `api.resend.com` locally. Other hosts are forwarded unchanged.

## Compatibility verification

Fabricate works through any client that can reach an API: `curl`, application
code, or an official SDK. We continuously test integrations to ensure they
behave as expected. The latest Resend check ran against the Acme Resend
environment with curl:

<IntegrationCompatibility compatibility={compatibility} />
